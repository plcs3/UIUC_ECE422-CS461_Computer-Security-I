# Escape Plan

**Perform dead analysis on suspect-l's disk:**

- Inspect ***home/clmustard/.purple/accounts.xml*** to find suspect-l's password ***leet123!@#***.

**Perform live analysis on suspect-l's computer:**

- Press Shift to enter recovery mode, and then select Ubuntu.
- Login as clmustard.
- Check mail inbox to find the contact email, escape time and location picture.
- Use exiftool to get the GPS info out of the location picture.
